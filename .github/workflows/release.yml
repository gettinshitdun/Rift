name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-matrix:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        include:
          - os: ubuntu-latest
            artifact-suffix: linux-x86_64
          - os: macos-latest
            artifact-suffix: macos-x86_64

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build binaries
        run: |
          make clean && make
          
          # Create distribution package
          mkdir -p rift-dist/bin
          cp rift-server rift-dist/bin/
          cp rift-client rift-dist/bin/
          cp README.md rift-dist/
          cp -r docs/ rift-dist/
          
          ls -lh rift-dist/bin/

      - name: Create tarball
        run: |
          tar czf rift-${{ matrix.artifact-suffix }}.tar.gz rift-dist/
          ls -lh rift-*.tar.gz

      - name: Create checksum
        run: |
          shasum -a 256 rift-${{ matrix.artifact-suffix }}.tar.gz > rift-${{ matrix.artifact-suffix }}.tar.gz.sha256
          cat rift-${{ matrix.artifact-suffix }}.tar.gz.sha256

      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: rift-builds
          path: |
            rift-*.tar.gz
            rift-*.tar.gz.sha256

  create-release:
    runs-on: ubuntu-latest
    needs: build-matrix
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download artifacts
        uses: actions/download-artifact@v3
        with:
          name: rift-builds
          path: ./releases

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: releases/*
          body: |
            # RIFT Release ${{ github.ref_name }}
            
            ## Changes
            See commit history for details.
            
            ## Installation
            
            Download the appropriate binary for your platform:
            - `rift-linux-x86_64.tar.gz` for Linux
            - `rift-macos-x86_64.tar.gz` for macOS
            
            ```bash
            tar xzf rift-<platform>.tar.gz
            cd rift-dist
            ./bin/rift-server
            ```
            
            ## Checksums
            
            Verify integrity:
            ```bash
            sha256sum -c rift-<platform>.tar.gz.sha256
            ```
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
